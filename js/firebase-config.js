// Paste your own Firebase project's config here to enable cross-device
// progress sync. See README.md → "Optional: sync progress across devices"
// for the full step-by-step setup (create a free Firebase project, enable
// Google sign-in, create a Firestore database, paste security rules, then
// paste your config below).
//
// Until you replace apiKey below, the app ignores this file entirely and
// works exactly as it does now — local-only, nothing broken, no setup
// required. This is fully optional.
//
// Your config comes from: Firebase Console → Project settings (gear icon)
// → General tab → "Your apps" → the web app's config object. It looks
// like the object below — just copy yours in as-is.

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
